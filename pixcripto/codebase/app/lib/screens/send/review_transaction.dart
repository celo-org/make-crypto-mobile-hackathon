import 'package:flutter/material.dart';

class ReviewTransaction extends StatelessWidget {
  const ReviewTransaction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
