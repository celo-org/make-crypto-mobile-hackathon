// @flow

import { connect } from 'react-redux';

// import { getUserDiagnostic } from '../../state/user';

import WebViewScreen from './screen';

const mapStateToProps = (state) => ({
});

export default connect(
  mapStateToProps
)(WebViewScreen);
