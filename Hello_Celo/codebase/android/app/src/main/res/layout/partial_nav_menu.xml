<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/vg_nav_drawer"
    android:layout_width="240dp"
    android:layout_height="match_parent"
    android:layout_gravity="start"
    android:background="?attr/colorOnPrimary"
    android:orientation="vertical"
    android:paddingTop="32dp">

    <LinearLayout
        android:id="@+id/vg_hero"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/iv_hero_icon"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="@dimen/default_padding_half"
            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize50Percent"
            android:scaleType="centerCrop"
             />

        <TextView
            android:id="@+id/tv_username"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="@dimen/default_padding"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            tools:text="My_USERNAME" />

        <View
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@color/black_25p" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_profile"
        style="@style/MenuOption"
        android:text="@string/profile_titlecase" />

    <TextView
        android:id="@+id/tv_friends"
        style="@style/MenuOption"
        android:text="@string/friends_titlecase" />

</LinearLayout>